server:
  port: ${sp.doc}
spring:
  application:
    name: ${sn.doc}
knife4j:
  enableAggregation: true
  nacos:
    enable: true
    serviceUrl: http://192.168.6.100:8848/nacos/
    routes:
      - name: 示例服务
        serviceName: ${sn.sample}
        namespaceId: ${nacos.namespace}
        location: /v2/api-docs?group=sample